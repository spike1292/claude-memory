{
  "name": "claude-memory",
  "version": "0.2.0",
  "private": true,
  "description": "Layered memory for Claude Code — local embedding runtime for scripts/memory-semantic.mjs",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@huggingface/transformers": "4.2.0"
  },
  "allowScripts": {
    "onnxruntime-node@1.24.3": true
  },
  "scripts": {
    "format": "npx --yes prettier@3.6.2 --write .",
    "format:check": "npx --yes prettier@3.6.2 --check ."
  }
}
